spring:
  datasource:
    driverClassName: org.postgresql.Driver    
    url: jdbc:postgresql://67.205.143.180/tcs2
    username: modulo4
    password: modulo4

server:
   port: 8080
   
eureka:
  client:
    serviceUrl:
      defaultZone: https://modulo-alumno-eureka.herokuapp.com/eureka #
  instance:
    hostname: localhost
    nonSecurePort: 8080

banner:
  location: classpath:/example/banner.txt